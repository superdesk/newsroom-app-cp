[{
    "_id": "reset_password_email",
    "init_version": 1,
    "subject": {
        "default": "{{ app_name }} - password reset",
        "translations": {
            "fr_ca": "{{ app_name }} - Réinitialisation de mot de passe"
        }
    }
}, {
    "_id": "account_created_email",
    "init_version": 1,
    "subject": {
        "default": "{{ app_name }} account created",
        "translations": {
            "fr_ca": "Compte {{ app_name }} créé"
        }
    }
}, {
    "_id": "new_wire_notification_email",
    "init_version": 1,
    "subject": {
        "default": "{{ item.headline }}",
        "translations": {
            "fr_ca": "{{ item.headline }}"
        }
    }
}, {
    "_id": "agenda_new_coverage_email",
    "init_version": 1,
    "subject": {
        "default": "{{ item.headline }}",
        "translations": {
            "fr_ca": "{{ item.headline }}"
        }
    }
}, {
    "_id": "share_items",
    "init_version": 2,
    "subject": {
        "default": "{% if items | length is gt 1 %} {{ items | length }} items shared with you{% else %}{{ subject_name }}{% endif %}",
        "translations": {
            "fr_ca": "{% if items | length is gt 1 %} {{ items | length }} éléments partagés avec vous{% else %}{{ subject_name }}{% endif %}"
        }
    }
}, {
    "_id": "share_agenda",
    "init_version": 2,
    "subject": {
        "default": "{% if items | length is gt 1 %} {{ items | length }} items shared with you{% else %}{{ subject_name }}{% endif %}",
        "translations": {
            "fr_ca": "{% if items | length is gt 1 %} {{ items | length }} éléments partagés avec vous{% else %}{{ subject_name }}{% endif %}"
        }
    }
}, {
    "_id": "share_topic",
    "init_version": 1,
    "subject": {
        "default": "Shared topic: {{ topic.label }}",
        "translations": {
            "fr_ca": "Sujet partagé: {{ topic.label }}"
        }
    }
}, {
    "_id": "agenda_updated_email",
    "init_version": 1,
    "subject": {
        "default": "{{ agenda.name or agenda.headline or agenda.slugline }} -{{ ' Coverage' if coverage_modified else '' }} updated",
        "translations": {
            "fr_ca": "{{ agenda.name or agenda.headline or agenda.slugline }} -{{ ' Couverture' if coverage_modified else '' }} mise à jour"
        }
    }
}]