[{
    "_id": "reset_password_email",
    "init_version": 2,
    "subject": {
        "default": "{{ app_name }} - password reset",
        "translations": {
            "fr_ca": "PC NouvellesPro - Réinitialisation de mot de passe"
        }
    }
}, {
    "_id": "account_created_email",
    "init_version": 2,
    "subject": {
        "default": "{{ app_name }} account created",
        "translations": {
            "fr_ca": "Compte PC NouvellesPro créé"
        }
    }
}, {
    "_id": "new_wire_notification_email",
    "init_version": 2,
    "subject": {
        "default": "{{ item.headline | safe }}",
        "translations": {
            "fr_ca": "{{ item.headline | safe }}"
        }
    }
}, {
    "_id": "new_agenda_notification_email",
    "init_version": 2,
    "subject": {
        "default": "{{ item.name | safe }}",
        "translations": {
            "fr_ca": "{{ item.name | safe }}"
        }
    }
}, {
    "_id": "agenda_new_coverage_email",
    "init_version": 2,
    "subject": {
        "default": "{{ item.headline | safe }}",
        "translations": {
            "fr_ca": "{{ item.headline | safe }}"
        }
    }
}, {
    "_id": "share_items",
    "init_version": 3,
    "subject": {
        "default": "{% if items | length is gt 1 %} {{ items | length }} items shared with you{% else %}{{ subject_name | safe }}{% endif %}",
        "translations": {
            "fr_ca": "{% if items | length is gt 1 %} {{ items | length }} éléments partagés avec vous{% else %}{{ subject_name | safe }}{% endif %}"
        }
    }
}, {
    "_id": "share_agenda",
    "init_version": 3,
    "subject": {
        "default": "{% if items | length is gt 1 %} {{ items | length }} items shared with you{% else %}{{ subject_name | safe }}{% endif %}",
        "translations": {
            "fr_ca": "{% if items | length is gt 1 %} {{ items | length }} éléments partagés avec vous{% else %}{{ subject_name | safe }}{% endif %}"
        }
    }
}, {
    "_id": "share_topic",
    "init_version": 2,
    "subject": {
        "default": "Shared topic: {{ topic.label | safe }}",
        "translations": {
            "fr_ca": "Sujet partagé: {{ topic.label | safe }}"
        }
    }
}, {
    "_id": "agenda_updated_email",
    "init_version": 5,
    "subject": {
        "default": "{{ (agenda.name or agenda.headline or agenda.slugline) | safe }} -{{ ' Coverage' if coverage_modified else '' }} updated",
        "translations": {
            "fr_ca": "{{ (agenda.name or agenda.headline or agenda.slugline) | safe }} - {% if coverage_modified %}Couverture mise à jour{% else %}Événement mis à jour{% endif %}"
        }
    }
}, {
    "_id": "coverage_request_email",
    "init_version": 3,
    "subject": {
        "default": "Coverage inquiry: {{ (item.name or item.slugline) | safe }}",
        "translations": {
            "fr_ca": "Requête de couverture: {{ (item.name or item.slugline) | safe }}"
        }
    }
}, {
    "_id": "company_expiry_email",
    "init_version": 1,
    "subject": {
        "default": "Companies expired or due to expire within the next 7 days ({{ expires_on }})",
        "translations": {
            "fr_ca": "Entreprises expiré ou doivent expirer dans les sept prochains jours ({{ expires_on }})"
        }
    }
}, {
    "_id": "company_expiry_alert_user",
    "init_version": 1,
    "subject": {
        "default": "Your Company's account is expiring on {{ expires_on }}",
        "translations": {
            "fr_ca": "Le compte de votre entreprise expirera le {{ expires_on }}"
        }
    }
}, {
    "_id": "additional_product_seat_request_email",
    "init_version": 1,
    "subject": {
        "default": "A customer {{ company.name }} has requested more seats for {{ app_name }}"
    }
}]